# Obstacle detected on the cell plate (world frame, metres)
string label          # e.g. "green"
float64 x
float64 y
float64 radius        # radius of the enclosing circle
float64 confidence    # 0..1 (blob area / expected area, clipped)
